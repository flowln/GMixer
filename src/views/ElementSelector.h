/*  This view is responsible for showing the available elements
 *  for the user to include in the pipeline.
 * */

#include <gtkmm/box.h>
#include <array>

class ElementSelector : public Gtk::Box {
    public:
        ElementSelector();
    private: 
};
